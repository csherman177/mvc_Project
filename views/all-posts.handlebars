

<ul class="list-group list-group-flush">
    {{#each posts as |post| }}
  <li class="list-group-item">
    <a href="/post/{{post.id}}">
    {{post.title}} Created On: {{post.createdAt}}</li>
    </a>
  {{/each}}
</ul>